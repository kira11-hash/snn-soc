# V2C 论文逻辑主线（motivation + 技术方案 + 评估，权威版）

> 用户 2026-06-06 认可的完整逻辑梳理。论文 intro/motivation 直接据此写。技术规格见 `plan-v1.md`，PPA 创新与调研见 `V2C_极致PPA创新点.md`（§A–§L），执行进度见 `python_multilayer/v2c/PROGRESS.md`，接力见 `V2C_接力文档_PhaseABC-RTL.md`。

---

## 一、漏斗式 motivation（从大背景到贡献）

**① 场景 & 大问题.** 面向**边缘 always-on 低功耗推理**。冯诺依曼架构存储与计算分离，数据来回搬，**搬运比计算更费电（memory wall）** → 边缘吃不消。

**② 两条互补的路（分清各自解决什么）.**
- **存内计算（CIM）**：存算一体，直接在存储里出结果 → 解决**搬运瓶颈**。
- **SNN / TTFS**：脉冲事件驱动、单脉冲稀疏、"谁先发谁赢"分类 → 解决**计算的功耗与延迟**（脉冲一出即往下传，不像 DNN 要算完整网才出结果 = 范式级延迟优势）。
- 我们把两者**结合**：CIM 砍搬运 + SNN 砍计算功耗。

**③ 器件：为什么 RRAM、为什么 0T1R.** SRAM-CIM 成熟可靠但 6T-8T 单元面积大、密度低；RRAM 非易失/高密度/低泄漏；我们的 **0T1R 是真实器件（组里发过 IEDM / Nature Communications）**，cell 0.64μm²@SMIC55nm，密度远胜 SRAM-CIM。**gap = 市面缺完整的 0T1R + SNN + 数字 CIM 的 SoC 级加速器** → 我们的位置。

**④ 计算范式：为什么数字 CIM、不用模拟 CIM.** 模拟 CIM（电流累加 + ADC）省电但 **器件 variation/drift/IR-drop 让结果飘、ADC 又大又费电、不可复现** → 鲁棒性差。我们用**纯数字 CIM**：cell 严格二值、popcount+移位加、1-bit 数字读出、**无 ADC**。代价是放弃模拟"一拍算一整列"的极致并行，**换来精确、抗噪声、可复现**（= 鲁棒性卖点：数字优雅降级 vs 模拟崩，有对照实验）。**针对 0T1R 的 CMOS 适配设计**（工程创新）：1-bit sense 数字读出替代 ADC、**V/2 半选通 + 双极性 P&V 写擦验证 FSM**（半选通防 0T1R 潜行电流/disturb）、读写外围电压方案。

**⑤ 算法 & 编码：为什么 dense 输入 + TTFS.** 部署 recipe = 先训量化 ANN，再用**解析公式把阈值搬进 SNN（零脉冲训练）**，已验证。**第一层必须 dense 多比特输入**：纯 TTFS 单脉冲输入会把图二值化、精度封死 ~68%；多比特 ramp 输入算出完整灰度 → ~87%。**隐层/输出层保持 TTFS 单脉冲稀疏**：低 SOP/低能耗/神经形态身份 + 首脉冲早停（对外 vs DNN 范式延迟优势）。

**⑥ 瓶颈 & 解法（核心）.** 瓶颈 = dense 第一层占 **99.8%** cycle 与绝大部分能耗（密集、无脉冲稀疏）。解法 = **输入跳零**（dense 输入里大片是零：背景黑 + 多位表示的零位）。三数据集实测 **省 73–88%**（25088→3028–6640 拍），且**无损**（跳的本来是加 0）→ 只改 RTL、不改算法、精度不掉。**闭环 motivation**：精度逼用 dense → dense 造瓶颈 → 但 dense 天然稀疏 → 输入跳零无损吃掉它。

**⑦ 创新点（三层）.** 算法层：ANN→SNN 解析 gate-init 零训练部署。架构层：读宽对齐的**输入跳零事件行串行数据通路** on 数字二值 0T1R + TTFS（+ RTL 实现阶段再挖的架构/数据流/调度创新）。器件适配层：1-bit 数字读出 + V/2 半选 P&V 写擦验证。**诚实定位**：单技巧不够新，**novelty = 这套组合 + 真硅化**；打鲁棒性/能效/延迟轴，不拼准确率。

**⑧ 评估方法（三桶，诚实标注）.** 见下「关键澄清 B」。

**⑨ 当前状态.** Python 参考模型全绿（167 测试）、3 个核心 RTL 模块与参考逐比特对齐、鲁棒性曲线完成；三轮 20+ subagent 调研 + Codex 8 路 + 真实权重实测，**双盲收敛到"输入跳零"为唯一真实主力**（其余花招全排除/被否，留痕 §G–§L）。

**⑩ 下一步.** 补 p95/worst-case cycle → 多 subagent 构思 RTL（极致 PPA + 架构/数据流创新）+ Codex 同步 → cross-check + 多方审核 + 参考 plan → 详细 RTL 开工 spec → 开工。

---

## 二、关键澄清

### A. 我们是"纯数字 CIM"，不是 mixed-signal 计算
**计算路径全数字**（popcount + 移位加，无 ADC、无模拟电流累加）。RRAM 只做**二值存储**；读出用 1-bit sense amp 只判"0 还是 1"（数字判决），**不是把模拟量化成多比特** → **没有模拟计算的非理想性**（抗噪声的根）。RRAM 阵列 + 读写驱动在 PPA 里单独估，只因它不是数字标准单元，**不是"模拟计算"**。
→ plan:30/59 里"mixed-signal 外围"措辞应改为"**RRAM 读写外围**"，免审稿人误读成有模拟计算。

### B. 三桶评估的精确边界（决定哪些实测/代入/估算）
判据 = **能不能映射到 SMIC55nm 数字标准单元**（不是"有没有 Verilog"——行为模型综合出的是 SRAM 等价物、不是真器件）。
| 桶 | 内容 | 怎么拿数 |
|---|---|---|
| ① **数字逻辑（大头）** | popcount/移位加/神经元/**读写数字控制 FSM（P&V/译码/retry/verify）**/跳零调度 等**全部数字电路** | **DC 综合实测** + FPGA 功能/实时验证 |
| ② **RRAM cell 阵列** | 忆阻器存储本身（DC 库里没有此器件） | 组里 **0T1R 器件实测数据（IEDM/NC）** 代入 |
| ③ **模拟外围（小头）** | 1-bit sense amp + WL/BL 高压驱动 + 电压生成（3.3/1.5/0.75V） | **estimate**（器件方/文献/custom） |
- ⚠ 关键认知：Verilog 里的 `cells_mem` 是**功能行为模型**（寄存器数组当 RRAM，给仿真/parity）；综合它得到 SRAM 等价物、**不是真 RRAM 的 PPA**。真 RRAM 物理指标只能用器件数据（桶②）。
- 好消息：我们 **1-bit sense、无 ADC** → 桶③特别小且干净（不像模拟 CIM 的多比特 ADC 是又大又飘的大头）→ "需估算"占比小、不确定性低，本身是相对模拟 CIM 的优势。
- Headline：**latency（RTL/FPGA 实测 cycle）为主竞争指标**、SOP/J projected、density 作 motivation；只在 **1–2 个具体维度**声称领先同行 + 标注评估条件差异。

### C. 数据记录原则：best + honest 双版本
任何结果都同时保留 **best-case（乐观/最佳样本）** 与 **honest/conservative（典型 mean + worst-case）** 两版，明确标注，不只留一个（如 cycle：best=最稀疏样本、typical=mean、worst=最坏样本，全报）。
