"""Vendored from V1 at 2026-04-19.

These modules are bit-for-bit copies of key functions from
``项目相关文件/器件对齐/Python建模/``. They are frozen on V2's side so
future V1 edits do not implicitly change V2 behavior.

MUST STAY bit-identical to V1 for:
    * V1 baseline parity (A1.2)
    * Python quantized-vs-hex parity (A6.a)
    * RTL-like multilayer reference (A2.5)

Do not edit these files. If V1 changes and V2 needs to follow, bump
``schema_version`` in ``topologies.yaml`` and re-vendor deliberately.
"""
