# V2C 器件参数 + 同学模拟-CIM SOW 参考（同一 0T1R 器件）

> 来源：同学的**模拟 CIM** 项目 SOW（同一两端口 0T1R RRAM 器件）。提取对我们**数字 CIM** 有用的器件参数 + 数字/模拟对照（novelty 金矿）+ 待确认项。2026-06-06。

## 1. 器件参数（我们能直接用，桶② 估算 + 论文器件表）
| 参数 | 值 | 用途 |
|---|---|---|
| 器件类型 | 两端口 RRAM = **0T1R** | 确认 |
| 权重精度 | 4-bit | = 我们 W=4 |
| **单 cell 读电流** | **10 nA（HRS/读"0"）~ 0.1 μA（LRS/读"1"）** | 桶② 读能耗估算、鲁棒性论证 |
| cell pitch | 0.8 μm（线宽0.4 + 间距0.4）→ **cell 面积 0.64 μm²** | 桶② 阵列面积 = 0.64μm² × cell 数 |
| 写/擦 | 3.3V、pulse 10μs、program/erase-and-verify | = plan:37 |
| 半选 | 1.5V（写时非目标 BL=1.5、擦时非目标 WL=1.5） | = plan |
| 读 | WL=1.5/BL=0（器件两端 1.5V）；半选抑制 WL=1.5/BL=0.75（两端 0.75V） | = plan |
| 输入频率 | 50 MHz | = plan |
- ⚠ 注意：cell 0.64μm² 由 **RRAM BEOL pitch（0.8μm）** 决定，**不是** SMIC55nm 逻辑 pitch。三桶里：阵列面积用 0.64μm²×cells（桶②），数字逻辑（popcount 等）用 55nm DC（桶①）。

## 2. ★ 数字 vs 模拟 CIM 对照（novelty 金矿：同器件 head-to-head）
| | 同学（模拟 CIM） | 我们（数字 CIM，V2C） |
|---|---|---|
| 读出 | TIA + ADC + 16:1 MUX | **1-bit sense（判 LRS/HRS），无 ADC** |
| 权重组织 | 正/负列分列、分别量化、数字域相减 | **two's-complement（MSB 取负），不分正负列** |
| 并行度 | **一次最多 16 行**（电流累加，受 ADC 动态范围限） | **不受此限**（1-bit sense 可整列并读 popcount），读宽由 sense-amp 数定 |
| 计算 | 模拟电流累加 | 数字 popcount + 移位加 |
| 非理想暴露 | 器件 variation / 电流噪声 / ADC 量化 / drift | **结构上零暴露**（数字精确）→ 鲁棒性卖点 |
- **★ 这是论文最强对照实验**：同一 0T1R 器件、同 4-bit 权重，模拟 vs 数字 head-to-head 比鲁棒性 / ADC 开销 / 能效 / 面积。比引文献硬。
- **数字侧真实优势点**：并行度不被 ADC 卡（同学限 16 行；我们可整列）；无 ADC（面积/能耗/抗量化）。
- ⚠ 诚实 trade-off：数字要 N 个 1-bit sense amp（多但小），模拟 16:1 MUX+1 TIA+ADC（少但大）；面积对比待 DC/估算。

## 3. ★ 待确认（架构前提，问老师/同学）
1. **阵列大小（最关键）**：同学阵列 **128×256**；我们 plan **1024×1024**；网络第一层 784→246 需 **784 行 × 984 列（246×4）**——同学阵列装不下我们的网络。**我们数字 CIM 用多大阵列？真流片 1024×1024 / projection（同器件外推）/ 还是网络缩到可流片尺寸？**
2. **读延迟 + 读能耗**：SOW 给了读电流（10nA-0.1μA）但没给"读一次的延迟 + 能量"——桶② 能效估算需要。
3. **数字侧 1-bit sense amp 谁设计**：同学做的是模拟 TIA+ADC（不是我们的）；我们数字 CIM 的 1-bit sense 阵列（桶③ 模拟外围）谁负责？这决定我们"读宽"自由度。

## 4. 已被 SOW 回答（不用再问老师）
读电流范围、cell pitch/面积、写擦读电压、半选策略、pulse 宽度、50MHz、P&V 流程、0T1R 确认——SOW 全给了。

> 下一步：Codex 回复 → 综合 → 定最终"问老师"清单 + 架构设计 + novelty。
