# V2C 极致 PPA 创新 — 深度独立调研请求（Codex#5；只读仓库、勿改代码）

> 复制全文给 Codex。它读 `~/dev/snn-soc/`：PPA 调研全在 `V2C_极致PPA创新点.md`（§A–§I）——可行性实测数据在 §F、我第一轮 4 路调研在 §G/§H、我第二轮 4 路 cross-check 在 §I；RTL 在 `rtl/v2c/`、Python golden 在 `python_multilayer/v2c/`（`cost.py` 有 projected-cycle 公式、`robustness.py`/`experiments.py` 有故障/实验）、架构规格在 `plan-v1.md`、决策记录在 `V2C_设计决策与权衡记录.md`。前几轮审查见 `V2C_Codex审查_*.md`。

## 0. 角色与任务（核心）
你是资深 数字 IC / CIM 数据通路 / 低功耗 / SNN-TTFS / DNN-DNN加速 审稿人 + 架构师。任务：**对"如何极致压榨这颗芯片瓶颈层（dense 第一层）的 PPA"做一轮深度、独立调研，验证 / 挑战 / 超越我（Claude）已做的两轮调研，给我一份非常详细的反馈 + 调研报告。**

★★ **用户硬性要求（务必照做）**：
1. **你必须派出 8 个独立 subagent**：4 路 × 每路 2 个。每路 2 个 agent **各自独立**调研同一路、再互相交叉对比（暴露分歧、别让它们抄彼此）。4 路 = ① DNN/CNN 硬件加速器；② LLM 加速算法/系统；③ 深度学习模型/算法侧（改网络/输入）；④ CIM 技巧 / 替代计算范式 / 编码。
2. **绝不拘泥于我的结论**。下面 §2 给出我的当前结论，**仅作"待你独立验证/挑战"的输入，不是让你附和**。独立检索文献、独立判断、**敢于推翻我**（我已经在第二轮 cross-check 推翻了自己第一轮的多个 headline，欢迎你继续推翻）。
3. **每完成调研做一次全面自检**；**不拘泥既定方案——只要发现能更极致压榨 PPA（面积/功耗/延迟）或拿到更大可发表 novelty 的，就提，哪怕要推翻我整个方向**。
4. 带具体工作 + 引用 + **真实迁移量级**：别把文献 headline 数（如某加速器 7.69×）当成我们能拿到的——必须判断迁移到我们这套约束（见 §1）后打几折，并说明理由。

## 1. 项目背景（硬约束）
V2C = **数字二值 0T1R RRAM 存内计算（CIM）+ TTFS 脉冲神经网络** 加速器。
- **器件/计算**：cell 严格二值（0/1）；一个 4-bit 权重 = 4 个二值 cell（two's-complement）；乘加 = popcount（数 1 的个数）+ 移位相加；**无模拟、无 ADC、无乘法器**——核心卖点 = 全数字 → 抗器件 variation/PVT/噪声鲁棒（区别于模拟 CIM）。物理阵列 1024×1024，**固定 128-bit 读宽（bit-parallel weight，32 输出/stripe）**；**RRAM write-once**（部署后难重写/重映射）。
- **网络/编码**：**超小 MLP 784→246→10**（Fashion-MNIST / KMNIST / MNIST）；TTFS 编码（每神经元每帧≤1 脉冲，分类靠"谁先发脉冲"= argmin 首发时间）；输入像素量化 4-bit、bit-serial 按 bitplane 喂第一层算 z1=Σ_k 2^k·popcountMAC(bitplane_k)；第一层 ramp-TTFS（membrane=(t+1)·z1 首次过阈发火），第二层 TTFS 积分。
- **部署 recipe（特殊、强约束）**：**不做端到端脉冲训练**；先训匹配的量化 ANN（1-bit 隐层激活），再**解析地**把阈值搬进 SNN（零脉冲训练）。任何"改第一层/改输入/改编码"都必须在 **ANN→SNN 解析映射 + 1-bit 隐层 + ReLU↔spike-time 单调映射** 下成立，不破坏零脉冲部署。
- **目标 & 定位**：极致 PPA + **可发表 novelty**。**accuracy 非胜负手**（~80% 够用，真正卖点是无 ADC 的抗噪声鲁棒性 + 低能耗/延迟），**可接受小精度换大 PPA，但要守 KMNIST ~80% 底线**（TTFS 比谁先发，近平局对误差敏感）。batch=1（单图边缘推理）。
- **实测瓶颈（关键）**：**第一层 784→246 的 dense 乘加占 99.8% 的 projected cycle（≈25088 = in_bits 4 × stripes 8 × in_dim 784）和绝大部分能耗**。它密集、无脉冲稀疏可用。第二层稀疏（fire-rate 22%）几乎免费。**所有 PPA 攻坚都聚焦砍这个 dense 第一层。**

## 2. 我已做两轮调研的当前结论（仅供你独立验证/挑战，别附和）
**第一轮（§G/§H，4 路 subagent）**提出"dense 第一层有 4 个正交可叠乘的面"：① 输入 skip-zero（跳零像素/零位，省 50-73%）② 权重 bit 稀疏（CSD/signed-digit + LUT 复用）③ 缩小瓶颈本身（降维 28²→14² + 降比特 4→2 + 低秩 784→r→246）④ 换计算物理（time-domain popcount）。

**第二轮 cross-check（§I，4 路新 subagent 独立挑战）推翻/降级了多个第一轮 headline**：
- **CSD 降级**：4-bit 下只 ~28%（非 33%）；DB-PIM 7.69× 是 8-bit SRAM 组合数，迁到 4-bit 二值 RRAM 现实 ~1.3-1.5×；三值 {-1,0,+1} 不适配 write-once 二值 cell。
- **截断/近似加法树"对 TTFS 免费" 被推翻**：仿真丢 1 个 LSB bitplane → top-1 翻转 ~9%（TTFS 近平局被误差翻序）→ 必须 per-output 有界。
- **LUT-GEMM/BiQGEMM 被推翻（净负）**：它复用的是激活跨 batch、假设 FP 激活 + 亿级矩阵 + 大 batch；我们 batch=1 / 4-bit 激活 / 246 小矩阵 → 建表一次用一次、build 开销主导。
- **time-domain popcount 被推翻（砸卖点）**：delay-chain 本质模拟时序，砸"全数字抗 PVT"卖点；arXiv 2403.18367 定量证严格精度下纯数字全面胜 TD；数字计数器折中后收益归零。
- **降维 1-2% 是 CNN 数**：1-bit 隐层 dense MLP 真实 3-6pp、叠加降比特超线性 5-9pp，KMNIST 可能破 80% 底线。**低秩降为高风险赌注**。**CREW 式复用对 FC 层不适用**（机理是权重重复、FC 无重复）。

**我当前收敛的主线（§I1，待你验证/挑战/超越）**：① **bit 级零跳过**（输入 skip-zero + 高 bitplane 零 bit skip + 离线列对齐 bit-零打包 arxiv 2511.14202，全数字/有硅证/零 PVT 代价）② **KD 蒸馏**找回 1-bit 精度 ③ **像素 codebook LUT**（输入端 16 值复用，需实测）④ 单轴降维 or 降比特 + 蒸馏（守 80% 底线）。

## 3. 你要回答的（每路 2 个 subagent，独立 + 交叉）
1. **验证**：我 §I1 收敛的主线，迁移到我们约束后**真实收益**多少？尤其 ①bit 级零跳过（高 bitplane 零 bit 率 + 列对齐重排能制造多少可跳零列，给真实 Fashion/KMNIST 权重的量级估计）、③像素 codebook LUT（246 输出权重各异下还省不省得到）。
2. **挑战**：我 §I0 的推翻对吗？有没有我**错杀**的（其实可救）、或我**仍高估**的？（例：CSD 真完全不值吗？降维真有 5-9pp 那么差吗？）
3. **超越**：有没有我两轮都没想到的、更极致 PPA + 更大 novelty 的招（架构/数据流/调度/编码/算法/近阈值/数据相关时钟…），契合"全数字二值 0T1R RRAM + popcount + 无 ADC + TTFS + batch=1 + 1-bit 隐层 + ANN→SNN 解析映射"？
4. **落地**：每条给到 RTL 或算法可实现的路线 + **怎么用真实数据/parity 验证**（我们一向用真实权重实测 + bit-exact parity，别只引文献量级）。

## 4. 诚实红线
- **全数字、不伤抗 PVT/抗噪声鲁棒卖点**（模拟时序/随机计算等砸卖点的，直接标注否决）。
- **真实迁移量级**：文献 headline 必须打折并说明理由（baseline/器件/位宽/batch 是否同我们）。
- **守 KMNIST ~80% 精度底线**；任何精度换 PPA 的招要给三数据集的真实预期代价。
- 不混仿真与硅；标注哪些有硅验、哪些仅仿真/估算。

## 5. 输出格式（要"非常详细"）
- **分 4 路**，每路给【2 个独立 subagent 各自发现】+【两者交叉的一致点 / 分歧点】。
- 每条结论标：`confirmed / challenged / new` + **真实迁移量级**（打折后）+ **novelty 判断**（在数字二值 0T1R RRAM-CIM + TTFS 上是否未发表）+ **落地路线** + **验证方法**。
- 最后给：**带优先级的 PPA 创新路线**（哪个先做）+ **最该先做实验验证的 3 项**（含怎么验）+ **你认为我两轮里最该被推翻/最被低估的各一条**。
- 我会综合你的报告 + 我的两轮，给用户大白话讲懂，**一起定创新点，然后去做具体实验验证**。
