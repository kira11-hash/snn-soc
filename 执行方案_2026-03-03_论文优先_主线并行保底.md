# 2026-03-03 起执行方案（论文优先 + 主线并行保底）

> 版本：v2026-03-03-final
> 目标：在 2026-06-30 前完成可投稿论文交付，同时不丢失 main 主线流片节奏。

## 1. 摘要
1. 执行节奏采用“论文优先 + 主线并行保底”。
2. 论文主结果固定为 ARM 完整量化；E203 固定为最小闭环演示。
3. 创新点评估必须采用 baseline→A/B 顺序，禁止未建基线先报优化收益。
4. 计数器采样框架/日志脚本是观测基础设施，可前置建设，但不等于创新点已实现。

## 2. 执行四原则（统一口径）
1. 先建立基线：先跑稳 ARM 主路径完整量化 + E203 最小闭环，拿到“无创新点”baseline数据。
2. 再做创新 A/B：同一数据集、同一时钟、同一量化配置下对比 OFF/ON。
3. 观测工具可前置：计数器采样框架/日志脚本先做，保证后续结果可比可复现。
4. E203 边界固定：首稿不做 E203 全量，E203 全量归入二期扩展。

## 3. 文档分工（执行入口）
1. `doc/14_fpga_fullversion_execution.md`：工程执行手册（怎么跑通、怎么 bring-up、怎么验）。
2. `doc/15_fpga_execution_master_plan.md`：总计划口径（做什么、何时做、验收到什么程度）。
3. `doc/13_fpga_paper_plan.md`：论文叙事与实验结构（主表/消融表、审稿口径）。
4. `doc/12_fpga_validation_guide.md`：验证清单模板（合入前/投前统一检查）。
5. `doc/06_learning_path.md`：学习路径（按需穿插，不后置到全部完成后）。
6. `SNNSoC工程主文档.md`：总文档补充口径（历史保留 + 截至日期更新）。

## 4. 阶段执行（绝对日期）
## 阶段 0：基线冻结（2026-03-03 ~ 2026-03-05）
1. 冻结 fpga 分支可复现基线（回归脚本、关键日志、文档版本）。
2. 清理“口径冲突”，确保 13/14/15/06/主文档一致。

## 阶段 1：FPGA工程链路跑通（2026-03-05 ~ 2026-03-15）
1. 跑通仿真链路：light + AXI + UART + SPI + CIM unit。
2. Linux + Vivado 完成 synth/place/route/bitstream。
3. 完成板级 bring-up（FSM/VIO/ARM 路径可选）。

## 阶段 2：ARM主路径完整量化（2026-03-15 ~ 2026-04-20）
1. ARM 端完成批量推理与日志导出。
2. 按统一采样口径收集精度/延迟/吞吐/计数器证据。
3. 形成论文主图主表（baseline版本）。

## 阶段 3：E203最小闭环（2026-04-20 ~ 2026-05-10）
1. 只做最小演示：初始化→触发→等待完成→读结果。
2. 不做 E203 全量大样本评测。

## 阶段 4：创新点 A/B 与中断增强（2026-05-01 ~ 2026-05-25）
1. 落地低风险创新点：稀疏跳过、稀疏统计、可观测性增强。
2. 中断模式增强：状态/掩码/清除/超时，异常可回退轮询。
3. 统一输出 OFF/ON 对比表（A/B 消融）。

## 阶段 5：论文收敛与投稿（2026-05-20 ~ 2026-06-20）
1. 完成方法、实验、消融、讨论、答辩问答口径。
2. 统一复现实验脚本与图表生成链。

## 阶段 6：main 并行保底（从 2026-04-15 起并行）
1. 以低风险增量推进 main：AXI/UART/SPI/DMA/E203。
2. 每个模块完成即走 `doc/12` 验证，不堆积到最后。
3. 目标是 6.30 前保留流片主线可持续推进能力。

## 5. 计数器采样框架 / 日志脚本（统一定义）
## 5.1 定义
1. 计数器采样框架：固定测量协议（何时读、怎么算、怎么算指标）。
2. 日志脚本：自动执行协议并产出可复现实验证据。

## 5.2 固定采样协议（必须）
1. 运行前读 `REG_DBG_CNT_0(0x30)`、`REG_DBG_CNT_1(0x34)` 作为 `before`。
2. 跑 N 帧后再读一次作为 `after`。
3. 每个 16-bit 子字段计算：`delta = (after - before) mod 65536`。
4. 禁止用瞬时寄存器值直接作图。

## 5.3 指标口径（固定公式）
1. `cycles_per_frame = delta_cim_cycle / max(delta_dma_frame, 1)`
2. `stall_ratio = delta_wl_stall / max(delta_cim_cycle, 1)`
3. `spike_per_frame = delta_spike / max(delta_dma_frame, 1)`

## 5.4 日志脚本最低输出字段
1. `run_id`、`git_commit`、`mode(ARM/E203)`、`dataset`、`clock_mhz`
2. `quant_cfg`、`frames`、`before_raw`、`after_raw`、`delta_*`
3. `accuracy`、`latency`、`throughput`

## 5.5 建议CLI（先文档定义，后续实现）
1. `perf_collect --mode {arm|e203} --frames N --quant CFG --out result.csv`
2. `perf_analyze --in result.csv --out summary.json`

## 5.6 常见错误（必须规避）
1. 不做差分，直接读瞬时值。
2. 忽略 16-bit 回绕。
3. 混入 warm-up 数据。
4. 跨配置横向比较（时钟/数据集/量化不一致）。
5. 不记录 commit，导致结果不可追溯。

## 6. 创新点执行顺序（硬约束）
1. 先 baseline（ARM 主结果）
2. 再 E203 最小闭环（系统完整性）
3. 最后创新 A/B（OFF/ON 对比）

说明：采样框架/日志脚本可前置，不计作“创新点实现完成”。

## 7. 公共接口与变更边界
1. 本方案不新增 RTL 功能接口（仅统一文档口径和实验协议）。
2. 实验寄存器口径固定：`0x30/0x34` 差分采样。
3. 中断增强寄存器（若实现）遵循：`IRQ_STATUS/IRQ_MASK/IRQ_CLEAR/TIMEOUT`。

## 8. 验收标准
## 8.1 工程验收
1. 仿真链路通过：light + AXI + UART + SPI + CIM unit。
2. FPGA链路可执行：synth/place/route/bitstream 可完成。

## 8.2 论文验收
1. 主结果：ARM 完整量化（>=3组量化配置，>=1000样本）。
2. 补充结果：E203 最小闭环稳定跑通。
3. 消融结果：至少 1 组创新 A/B 对比表。
4. 图表可复现：脚本可重复生成主图主表。

## 8.3 顺序门槛
1. 未完成 baseline，不允许报告优化收益。
2. E203 不做全量不影响首稿通过门槛。

## 9. 风险与回退
1. 若中断模式影响稳定性，回退轮询模式，不阻断主实验。
2. 若某创新点影响时序，先下线该创新点，保主结果进度。
3. 若主线并行导致资源冲突，优先论文交付，main保持小步增量。

## 10. 今日执行起点（按本方案）
1. 先按 `doc/14` 跑通当前 FPGA 路径并锁定 baseline。
2. 立即建立计数器采样脚本模板和日志字段。
3. 进入 ARM 完整量化数据采集。
4. ARM 稳定后补 E203 最小闭环。
5. 最后做创新 A/B 消融并收敛论文。
